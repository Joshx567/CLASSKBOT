import multiplicar from "./multiplicar.js";

describe("multiplicar", () => {
  it("debería multiplicar dos números", () => {
    expect(multiplicar(10, 10)).toEqual(100); // Corrección: usar multiplicar y resultado correcto
  });

  
});